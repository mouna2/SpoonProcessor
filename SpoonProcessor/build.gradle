apply plugin: 'java'

repositories {
    mavenCentral()
    flatDir {
    	dirs 'lib'
    }
}

dependencies {
    compile 'fr.inria.gforge.spoon:spoon-core:6.2.0'
    compile 'core-1.0.0.jar'
    compile 'monitoring-1.0.0.jar'
}